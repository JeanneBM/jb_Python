continue – przerywa obieg pętli, pętla przechodzi bezpośrednio do kolejnego obiegu

imie = "Asia"

for l in imie:
  if l=="i":
    continue
  print (l)

else:
  print ("Koniec")
  
-----------------------------------------------------------------------------  

lista = [13, "kot", 4, "pies", 1.5]
ile = 0
for element in lista:
    if element == "pies":
        continue
    ile = ile + 1
print(ile)

-----------------------------------------------------------------------------

napis = "technikinformatyk"
ile = 0
for litera in napis:
    if litera == "k":
        continue
    else:
        ile = ile + 1
print(ile)

-----------------------------------------------------------------------------

lista = [4, 5, 9, 3, 5, 3, 7, 1] 
ile = 0
for numer in lista:
    if numer == 3 or numer == 5:
        continue
    else:
        ile = ile + numer
print(ile)

-----------------------------------------------------------------------------

